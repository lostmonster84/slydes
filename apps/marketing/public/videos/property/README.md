Drop property-tour clips in this folder to match the `DashboardPreview` walkthrough frames.

Expected filenames (mp4):
- `hero.mp4` — hero hook / exterior / best shot
- `living-room.mp4` — living room walkthrough shot
- `kitchen.mp4` — kitchen walkthrough shot
- `bedrooms.mp4` — bedrooms walkthrough shot
- `location.mp4` — map-style pan / neighbourhood / exterior/location vibe
- `book-viewing.mp4` — “final CTA” clip (can reuse hero if needed)

Notes:
- Keep clips **vertical** if possible (9:16). Horizontal still works; we use `object-cover`.
- Aim for 3–7 seconds each; they loop silently.
- If a file is missing, the UI will automatically fall back to `/videos/maison.mp4`.

