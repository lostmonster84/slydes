// Re-export client
export { supabase, createServerClient } from './client'

// Re-export types
export type {
  User,
  Slyde,
  Slide,
  SlideCTA,
  Media,
  Subscription,
  WaitlistEntry,
} from '@slydes/types'
